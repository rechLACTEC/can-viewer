[project]
name = "can-monitor"
version = "0.1.0"
description = "Backend Python para o monitor CAN."
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["uv_build>=0.9.9,<0.10.0"]
build-backend = "uv_build"
